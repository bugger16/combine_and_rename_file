# 📂 File Merger Automation Script

สคริปต์ Python สำหรับจัดการไฟล์แบบอัตโนมัติ โดยการเดินเข้าไปใน Folder ย่อย ค้นหาไฟล์ตามรูปแบบที่กำหนด และรวมไฟล์เข้าด้วยกันพร้อมตั้งชื่อใหม่ตามโครงสร้าง Folder (เดือน/วัน)

## ✨ คุณสมบัติ
- **Smart Merging**: รวมหลายไฟล์เป็นไฟล์เดียว พร้อมตัด Header ของไฟล์ที่ 2 เป็นต้นไปออกอัตโนมัติ
- **Dynamic Naming**: ตั้งชื่อไฟล์ใหม่ตามชื่อ Folder ชั้นบน (เช่น `202601_01_Report.csv`)
- **Safety First**: 
  - ข้ามโฟลเดอร์ที่เคยรันไปแล้วอัตโนมัติ (Idempotent)
  - ข้ามไฟล์ว่าง (0 bytes) เพื่อป้องกันโปรแกรมพัง
  - รองรับการเข้ารหัส UTF-8 สำหรับภาษาไทย
- **CLI Support**: สั่งงานผ่าน Command Line ได้ทันที

## 🚀 วิธีการใช้งาน

### 1. การเตรียมความพร้อม
ตรวจสอบว่าเครื่องของคุณติดตั้ง Python 3.x แล้ว

### 2. รูปแบบคำสั่ง
```bash
python file_manager.py [Directoryหลัก] "[รูปแบบชื่อไฟล์]" "[ชื่อที่จะต่อท้าย]"
